# SpaceO 1.1.1 live qualification

Source commit: `8c3abe4fc910331de627121b953b0ef6ef50cf74` (mirrored and high-refresh displays
admitted; incident qualification build removed). Later commits on this release change only
documentation.

Tester: implementer (Claude Code) on the owner's existing graphical login, by owner direction
on 2026-09-26. This is implementer qualification on an existing login, not independent or
fresh-user research.

## Host

| Field | Value |
|---|---|
| Hardware | Mac17,9, Apple M5 Pro (arm64) |
| macOS | 27.2, build 26B5091g |
| Displays | AW3225QF 3840×2160 (mirror master) and built-in Retina (hardware mirror) |
| TCC | Accessibility and Screen Recording granted, attributed to the hosting terminal app |
| Toolchain | Swift 6.4 (local); CI pins Xcode 26.3 / Swift 6.2 |

## Recovery before the run

The failure latch from the earlier September 26 run (see
[display containment](2026-09-26-display-containment.md)) was archived only after `spaceo doctor`
and `spaceo session list` showed no sessions, no SpaceO displays, and no process holding the
journal. The archived journal is retained locally, not committed.

## Results

All times IST, September 26, 2026. The normal build was used; no qualification compiler flag or
environment bypass exists any more.

| Check | Result |
|---|---|
| `make verify-release` | 1,624 deterministic Swift tests, supervisor/Node fixtures, and 34-tool MCP smoke passed |
| `make release-check`, `make release-dry-run` | Passed |
| Release-security and live-gate fixtures; warnings-as-errors release build | Passed |
| Supervised single lifecycle (`testStageCreateAndDestroyLeavesNoDisplay`) | 1 passed; no remaining virtual display or topology change |
| `SPACEO_LIVE_TESTS=1 make test-live-full` | **16 executed, 16 passed, 0 failed, 0 skipped** (1,472 s); completeness checker passed |
| `computer-use-check.mjs --suite=all --require-full` against a daemon built from the same source | **36/36 passed**, including Electron pre-launch refusal |
| Postflight | No SpaceO displays; mirrored topology unchanged; `doctor` ready; pre-existing daemon untouched |

The two-session TextEdit case that failed in the previous run passed. The intermittent
Accessibility blackout (BUG-9) was not reproduced; it is not proven fixed.

The matrix initially attached to the pre-existing shared daemon, an older installed build, and
stopped at its preflight before creating any display. It was rerun once against a throwaway
daemon from the tested build on a separate socket; that daemon was stopped afterwards.

## Retained evidence

Owner-only local artifacts in `.artifacts/live-20260926/`: single-lifecycle and full live logs,
the matrix log and structured action report, the throwaway daemon log, and the archived failure
journal. Raw diagnostics and user content are not committed.
