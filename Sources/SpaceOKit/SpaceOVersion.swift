/// The public SpaceO release version.
///
/// Keep this value in lockstep with the repository's `VERSION` file. Release preflight verifies
/// the pair before any artifact is built or signed.
public enum SpaceOVersion {
    public static let current = "1.0.0"
}
